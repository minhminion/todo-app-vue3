export enum AppTheme {
  LIGHT = "light",
  DARK = "dark",
}

export enum ActionTypes {
  GET_THEME = "GET_THEME",
}

export enum MutationTypes {
  TOGGLE_THEME = "TOGGLE_THEME",
  SET_THEME = "SET_THEME",
  LOADIND_START = "LOADIND_START",
  LOADIND_STOP = "LOADIND_STOP",
}
